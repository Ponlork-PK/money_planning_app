name: money_planning_app
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.3
  intl: ^0.20.2
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  fl_chart: ^0.71.0
  calendar_date_picker2: ^2.0.1
  supabase_flutter: ^2.12.0
  http: ^1.6.0
  uuid: ^4.5.2
  flutter_dotenv: ^6.0.0
  connectivity_plus: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
