import 'package:flutter/material.dart';

class BaseColors {
  // Brand blues
  static const primary = Color(0xFF0797FF); // main blue
  static const primaryLight = Color(0xFF4DB6FF);
  static const primaryDark = Color(0xFF0267B5);

  // Light mode
  static const background = Color(0xFFF5F5F5); // screen bg
  static const card = Colors.white;
  static const iconColor = Color(0xFF0797FF);
  static const itemIconBg = Color(0xFFD9D9D9);

  // Dark mode
  static const darkBackground = Color(0xFF050B18);
  static const darkCard = Color(0xFF0F172A);
  static const darkCardBorder = Color(0xFF1F2937);

  // Status
  static const income = Color(0xFF34B230); // green (Income, success)
  static const expense = Color(0xFFE24343); // red (Expense)

  // Appbar
  static const appBarTitle = Colors.white;

  // Text
  static const textPrimary = Color(0xFF222222);
  static const textSecondary = Color(0xFF777777);

  static const darkTextPrimary = Colors.white;
  static const darkTextSecondary = Color(0xFF9CA3AF);

  static const divider = Color(0xFFE0E0E0);
  static const darkDivider = Color(0xFF1F2933);

  static const inputFieldBg = Color(0xFFE9ECEF);
  static const darkInputFieldBg = Color(0xFF1F2933);

  // Active Color for buttons
  static const activeColor = Color(0xFF34B230);
}